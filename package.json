{
  "name": "@obvious/monorepo",
  "scripts": {
    "build": "turbo build",
    "lint": "turbo lint",
    "format": "pnpm -r run --if-present format"
  },
  "packageManager": "pnpm@10.5.2",
  "devDependencies": {
    "prettier": "^3.4.2",
    "turbo": "^2.4.0"
  }
}
