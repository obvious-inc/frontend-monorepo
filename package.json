{
  "name": "@obvious/monorepo",
  "scripts": {
    "build": "turbo build",
    "lint": "turbo lint",
    "format": "pnpm -r run --if-present format"
  },
  "packageManager": "pnpm@9.15.3",
  "devDependencies": {
    "prettier": "^3.3.3",
    "turbo": "^2.1.1"
  }
}
