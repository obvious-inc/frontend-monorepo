{
  "version": "1.0.0-alpha.32",
  "name": "@lilnouns/lilnouns-camp",
  "scripts": {
    "build": "turbo build --filter='./packages/*'",
    "postbuild": "cd apps/nouns-camp && pnpx @cloudflare/next-on-pages",
    "lint": "turbo lint",
    "format": "pnpm -r run --if-present format",
    "test": "pnpm -r run --if-present test"
  },
  "resolutions": {
    "@babel/traverse": ">=7.23.2",
    "axios": ">=1.6.0",
    "follow-redirects": ">=1.15.4",
    "ip": ">=1.1.9",
    "undici": ">=5.26.2"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^1.12.5"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.7.5"
  },
  "packageManager": "pnpm@8.15.1"
}
